# Core dependencies
numpy>=1.24.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# AI Data Remediation Engine dependencies
sentence-transformers>=2.2.0  # Local embeddings (no API calls)
chromadb>=0.4.0              # Local vector database  
pandas>=1.3.0                # DataFrame operations for audit trails

# Optional: Local LLM integration (install when available)
# ollama>=0.1.0              # Local SLM inference (phi3, llama3, mistral)

# Production monitoring (when deploying)
# structlog>=22.0.0          # Structured logging
# opentelemetry-api>=1.15.0  # Observability tracing
