import micropip
import numpy as np

# OptiGraph doesn't need external packages beyond NumPy (included in Pyodide)
# NumPy is built into Pyodide, so no installation needed

print("OptiGraph: Pyodide initialized with NumPy")
